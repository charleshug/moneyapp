<% provide(:title, "Vendors") %>
<%= render 'form', vendor: @vendor %>
<%= form_with(model: @vendor, local:true, method: "delete") do |f| %>
  <%= f.submit 'Delete' %>
<% end %>
<%= link_to 'Vendors Index', vendors_path %>